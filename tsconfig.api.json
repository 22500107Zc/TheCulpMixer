{
  // Type-checking only. The real settings live in api/tsconfig.json, which is
  // also the one Vercel finds when it builds the functions — so what this
  // checks and what production compiles are the same rules, by construction.
  "extends": "./api/tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": ["api"]
}
